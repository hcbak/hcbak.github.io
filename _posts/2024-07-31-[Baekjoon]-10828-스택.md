---
title: "[Baekjoon] 10828 스택"
author: hcbak
date: 2024-07-31 07:08:11 +0900
categories: [Algorithm, Baekjoon]
---


## Python
```python
def push(stack, x):
    return stack.append(x)

def pop(stack):
    sSize = len(stack)
    if sSize == 0:
        return print(-1)
    else:
        print(stack[sSize])
        return stack[:sSize - 1]
    
def size(stack):
    return len(stack)

def empty(stack):
    sSize = len(stack)
    if sSize == 0:
        print(1)
    else:
        print(0)

def top(stack):
    sSize = len(stack)
    if sSize == 0:
        print(-1)
    else:
        print(stack[sSize])


nCount = int(input())
N = []

for i in range(nCount):
    input(N.append(i))

```
